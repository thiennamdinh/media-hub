[project]
name = "media-hub-mastodon-worker"
version = "0.1.0"
description = "Mastodon/ActivityPub social worker for media-hub"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
  "PyYAML>=6.0.1"
]

[project.scripts]
media-hub-mastodon = "media_hub_mastodon.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
